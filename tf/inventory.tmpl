all:
  children:
    webservers:
      children:
        aws:
          hosts:
            AWS_PUBLIC_IP:
              ansible_ssh_private_key_file: ../tf/aws.pem
              ansible_user: ubuntu
        azure:
          hosts:
            AZURE_PUBLIC_IP:
              ansible_ssh_private_key_file: ../tf/azure.pem
              ansible_user: adminuser